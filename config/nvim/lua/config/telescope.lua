require('telescope').setup {
  defaults = {
    layout_strategy = 'center',
    -- mappings = {
    --   i = {
    --     ["<C-h>"] = "which_key"
    --   }
    -- }
  },
  -- pickers = {
  -- },
  -- extensions = {
  -- }
}
