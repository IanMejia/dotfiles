#!/bin/bash

# variables
xresources=$HOME/.Xresources
session=${1:-bspwm}

case $session in
    bspwm    ) exec bspwm;;
    *        ) exec $1;;
esac

# resources
if [-f $xresources]; then
    xrdb -merge $xresources
fi
